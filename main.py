"""Modular RAG MCP Server — application entry point."""

from __future__ import annotations


def main() -> None:
    """Minimal runnable entry (skeleton). Business logic added in later tasks."""
    print("Modular RAG MCP Server — skeleton ready")


if __name__ == "__main__":
    main()
